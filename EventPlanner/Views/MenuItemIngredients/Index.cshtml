@{
  Layout = "_Layout";
}

<h1 id="pageTitle">FOOD ITEMS</h1>
<h5>Click on a Food Item to See its Details</h5>
<h5><a href="/menuItemIngredients/new">Add a New Food Item</a><h/h5>
@if (Model.Count != 0)
{
  <h5><a href="/menuItemIngredients/delete">Delete All Food Items</a></h5>
}
<br>
<br>

@if (Model.Count != 0)
{
  @foreach (var menuItemIngredients in Model)
  {
    <ul class="list-group list-group-horizontal">
      <li class="list-group-item"><a href='/menuItemIngredients/@menuItemIngredients.GetId()'>@menuItemIngredients.GetIngredientDescription()</a></li>
      <li class="list-group-item"><a href="/menuItemIngredients/@menuItemIngredients.GetId()/edit">EDIT</a></li>
      <li class="list-group-item"><a href='/menuItemIngredients/@menuItemIngredients.GetId()/delete'><i class="far fa-times-circle"></i></a></li>
    </ul>
  }
}
